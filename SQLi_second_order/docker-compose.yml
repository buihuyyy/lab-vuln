version: "3.8"

services:
  app:
    build: .
    container_name: sqli-second-order
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DB_PATH=/app/data/app.db
    volumes:
      - app_data:/app/data
    restart: unless-stopped

volumes:
  app_data:
