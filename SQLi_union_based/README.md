Bài này hãy làm như 1 bài blackbox, không nên đọc sources code để có thể luyện được hướng đi

hint (nên thử tự làm trước khi đọc hint):
đầu tiên là phát hiện ra ta có thể thoát khỏi vùng truy vấn bằng dấu ' và sử dụng các câu lệnh SQL mà ta chèn vào
-> ta cố gắng tìm xem database là database gì bằng các truy vấn đặc trưng của từng loại database
-> ta sử dụng các câu lệnh truy vấn để lộ tên các bảng có trong database
-> sau đó ta sẽ truy vấn các bảng tìm được để ra tên các cột
-> rồi trích data từ tên của các cột tìm được ra thì sẽ ra được flag