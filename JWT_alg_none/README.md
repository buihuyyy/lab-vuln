cách chạy lab : Tạo 1 file .env theo file file mẫu .env.example-> npm start -> truy cập đường link
Bài này ta nên làm theo hướng blackbox, đây là 1 chall mô phỏng lỗ hổng JWT ở mức cơ bản là chỉnh sửa header alg->none để server sẽ không xác thực chữ ký và tin tưởng hoàn toàn vào header và payload
